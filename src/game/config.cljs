(ns game.config)

;; the screen that will be shown when the app loads
(def initial-view :heroes)

;; the initial number of monsters spawned
(def initial-hero-count 50)

;; the initial number of heroes spawned
(def initial-monster-count 10)

;; the gold that the player begins the game with
(def initial-player-gold 5000)

;; the number of heroes the player is allowed to own
(def player-hero-limit 5)

;; the number of items the player can store
(def player-inventory-size 28)

